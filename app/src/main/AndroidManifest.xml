<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobbile.paul.bcmobiletrader">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.mobbile.paul.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.attendant.Attendant"></activity>
        <activity android:name=".ui.customer.CustomersActivities" />
        <activity android:name=".ui.module.ModuleActivity" />
        <activity android:name=".ui.customers.CustomersActivity" />
        <activity android:name=".ui.productlist.ProductListActivity" />
        <activity android:name=".ui.salesentries.SalesEntryActivity" />
        <activity android:name=".ui.mainlogin.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>